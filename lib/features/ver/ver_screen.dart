import 'package:flutter/material.dart';

class VerScreen extends StatelessWidget {
  const VerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Zona de Ver 🗺️', style: TextStyle(fontSize: 24))
    );
  }
}