import 'package:flutter/material.dart';

class OrganizarScreen extends StatelessWidget {
  const OrganizarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Zona de organización 📋', style: TextStyle(fontSize: 24)),
    );
  }
}