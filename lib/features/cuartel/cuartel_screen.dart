import 'package:flutter/material.dart';

class CuartelScreen extends StatelessWidget {
  const CuartelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pantalla de Cuartel 🛡️', style: TextStyle(fontSize: 24)),
    );
  }
}
